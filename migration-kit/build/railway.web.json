{
  "$schema": "https://railway.app/railway.schema.json",
  "//": "Drop at repo root as railway.json for the WEB service. Adjust the start path if your Nitro output differs (confirm .output/server/index.mjs exists after build).",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "node .output/server/index.mjs",
    "healthcheckPath": "/",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "numReplicas": 1,
    "//numReplicas": "Keep at 1 until the Tier-2 single-flight lock lands (DIAGNOSIS C12) — extra replicas duplicate inline syncs and cache computes."
  }
}
